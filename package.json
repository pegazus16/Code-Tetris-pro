{
  "name": "code-tetris-pro",
  "version": "4.4.0",
  "description": "Jeu éducatif de frappe de code - Version Mobile",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html",
    "cap:init": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:run": "npx cap run android",
    "build:apk": "npm run build && npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "keywords": [
    "game",
    "educational",
    "coding",
    "typing",
    "mobile"
  ],
  "author": "pegazus16",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^5.5.1",
    "@capacitor/core": "^5.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.1"
  },
  "capacitor": {
    "ios": {
      "minVersion": "13.0"
    },
    "android": {
      "minVersion": "22"
    }
  }
}
